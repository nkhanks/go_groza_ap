
import 'package:flutter/cupertino.dart';

class Col12 extends StatelessWidget {
  final child;
  const Col12({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 1,
      child: child,
    );
  }
}

class Col11 extends StatelessWidget {
  final child;
  const Col11({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 0.9166,
      child: child,
    );
  }
}

class Col10 extends StatelessWidget {
  final child;
  const Col10({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 0.8333,
      child: child,
    );
  }
}

class Col9 extends StatelessWidget {
  final child;
  const Col9({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 0.75,
      child: child,
    );
  }
}

class Col8 extends StatelessWidget {
  final child;
  const Col8({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 0.6666,
      child: child,
    );
  }
}

class Col7 extends StatelessWidget {
  final child;
  const Col7({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 0.5833,
      child: child,
    );
  }
}

class Col6 extends StatelessWidget {
  final child;
  const Col6({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 0.50,
      child: child,
    );
  }
}

class Col5 extends StatelessWidget {
  final child;
  const Col5({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 0.4166,
      child: child,
    );
  }
}

class Col4 extends StatelessWidget {
  final child;
  const Col4({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 0.3333,
      child: child,
    );
  }
}

class Col3 extends StatelessWidget {
  final child;
  const Col3({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 0.25,
      child: child,
    );
  }
}

class Col2 extends StatelessWidget {
  final child;
  const Col2({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 0.1666,
      child: child,
    );
  }
}

class Col1 extends StatelessWidget {
  final child;
  const Col1({Key key, this.child}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width * 0.0833,
      child: child,
    );
  }
}
